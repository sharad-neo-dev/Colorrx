<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Coming Soon | Ace11</title>

  <!-- Mobile Responsive -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

  <!-- Remix Icon -->
  <link href="https://cdn.jsdelivr.net/npm/remixicon/fonts/remixicon.css" rel="stylesheet">

  <style>
    * {
      box-sizing: border-box;
    }

    html, body {
      margin: 0;
      padding: 0;
      width: 100%;
      height: 100%;
      background: radial-gradient(circle at top, #1a0f2e, #000);
      font-family: Arial, sans-serif;
      overflow: hidden;
      color: #fff;
    }

    /* HEADER */
    .header {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      height: 70px;
      background: rgba(20, 10, 40, 0.85);
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 12px;
      z-index: 1000;
      backdrop-filter: blur(8px);
    }

    .header-left {
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .header-left img {
      height: 45px;
    }

    .header-right {
      display: flex;
      align-items: center;
      gap: 8px;
      color: #c5a3ff;
      cursor: pointer;
      font-size: 16px;
    }

    .header-right img {
      width: 32px;
      height: 32px;
      border-radius: 50%;
    }

    button.back-btn {
      background: none;
      border: none;
      color: #fff;
      font-size: 26px;
      cursor: pointer;
    }

    /* COMING SOON CONTAINER */
    .coming-soon {
      height: 100%;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 20px;
    }

    .coming-soon i {
      font-size: 90px;
      color: #ffd700;
      margin-bottom: 20px;
    }

    .coming-soon h1 {
      font-size: 42px;
      margin-bottom: 12px;
      letter-spacing: 1px;
    }

    .coming-soon p {
      font-size: 18px;
      color: #d1c4ff;
      max-width: 500px;
      line-height: 1.6;
    }

    .coming-soon .badge {
      margin-top: 20px;
      padding: 10px 18px;
      background: linear-gradient(135deg, #6a11cb, #2575fc);
      border-radius: 30px;
      font-size: 14px;
      letter-spacing: 0.5px;
    }

    /* MOBILE */
    @media (max-width: 600px) {
      .header {
        height: 60px;
      }

      .header-left img {
        height: 35px;
      }

      .coming-soon h1 {
        font-size: 32px;
      }

      .coming-soon p {
        font-size: 15px;
      }

      .coming-soon i {
        font-size: 70px;
      }
    }
  </style>
</head>
<body>

  <!-- HEADER -->
  <div class="header">
    <div class="header-left">
      <button class="back-btn" onclick="window.location.href='/'" aria-label="Back to Home">
        <i class="ri-arrow-left-s-line"></i>
      </button>
      <img src="/img/Home/daman-red.png" alt="Logo">
    </div>

    <div class="header-right" onclick="window.location.href='/wallet/recharge'">
      <span>Add Cash</span>
      <img src="/xzcaszcasZ.png" alt="Wallet">
    </div>
  </div>

  <!-- COMING SOON CONTENT -->
  <div class="coming-soon">
    <i class="ri-gamepad-line"></i>
    <h1>Coming Soon</h1>
    <p>
      This game is currently under maintenance or getting exciting new updates.
      Please check back shortly.
    </p>
    <div class="badge">Launching Very Soon 🚀</div>
  </div>

</body>
</html>
